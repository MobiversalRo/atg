# Supabase connection (local: from `supabase status`; cloud: from project settings -> API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only (no NEXT_PUBLIC prefix). Required for the Users admin page.
SUPABASE_SERVICE_ROLE_KEY=
